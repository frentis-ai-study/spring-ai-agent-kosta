server:
  port: 8080

spring:
  application:
    name: spring-ai-agent-kosta-step1

  datasource:
    url: jdbc:postgresql://localhost:5432/agentdb
    username: agent
    password: ${POSTGRES_PASSWORD:agent}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-5.4-mini
          temperature: 0.0

logging:
  level:
    org.springframework.ai: INFO
    com.kosta.agent: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
